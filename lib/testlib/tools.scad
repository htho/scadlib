/**
 * This File defines an important function.
 * This comment won't be avaialable with --as-scad because the filename
 * attribute is missing! But it will be visible with --as-dump
 */


/** a function */
function r_from_dia(dia) = dia / 2;
